
/*
    this functions needs to be implemented by the developer to check if a user is connected
    tipically it assigns a session id to a user id to identify the user, maybe with a mysql table
    in this case we use use as example a table called session

 */
export function userTokenCheck(token) {

}
